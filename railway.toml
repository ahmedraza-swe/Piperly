# Piperly on Railway — Docker build from repo root
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# No healthcheckPath — first boot runs migrate/seed and can exceed check window
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
