# Piperly on Railway — Docker build from repo root
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Disabled: migrations/seed can take longer than healthcheck window on first deploy
healthcheckPath = ""
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
